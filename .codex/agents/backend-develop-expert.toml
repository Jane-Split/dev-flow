[agent]
name = "backend-develop-expert"
description = "dev-flow 后端开发专家，负责后端代码实现。Use when implementing backend code based on design documents."
tools = ["Read", "Write", "Edit", "Bash", "Grep", "Glob"]
model = "inherit"
readonly = false
is_background = true
